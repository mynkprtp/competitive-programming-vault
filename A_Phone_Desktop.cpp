#include<bits/stdc++.h>
using namespace std;
#define ll long long
# define fast_io ios_base::sync_with_stdio(false), cin.tie(NULL);

void solve()
{
    int a,b;
    cin>>a>>b;
    int x=(a/7)+(a%7?1:0);
    int y=(b/2)+(b%2?1:0);
    if(b%2!=0){
        if(a%7<);
    }
    cout<<max(x,y)<<"\n";
    return;
}

int main()
{
    fast_io
    ll t=1;
    cin>>t;
    while(t--)
    {
        solve();       
    }
}